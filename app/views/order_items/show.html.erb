<p id="notice"><%= notice %></p>

<p>
  <strong>Item:</strong>
  <%= @order_item.item.code %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @order_item.quantity %>
</p>

<%= link_to 'Edit', edit_order_item_path(@order_item) %> |
<%= link_to 'Back to Order', order_order_items_path(order_id: @order_item.order_id) %>
